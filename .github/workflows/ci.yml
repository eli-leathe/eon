name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  zig:
    name: Zig tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: mlugg/setup-zig@v2
        with:
          version: master
      - run: zig build test
